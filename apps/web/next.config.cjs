/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // przenieś tu swoje wcześniejsze opcje, ale bez TS (żadnych "satisfies", ": NextConfig", itp.)
};

module.exports = nextConfig;
